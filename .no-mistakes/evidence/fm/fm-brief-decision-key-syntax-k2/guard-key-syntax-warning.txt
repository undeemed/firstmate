### 1. A worker writes the WRONG form: the [key=x] token AFTER the colon
$ cat state/t31.status
needs-decision: pick A or B [key=x]
needs-decision: ship now or wait [key=y]

### 2. Drain: both fold under the shared "default" key (ONE collapsed record) and the guard names each lost key
$ bin/fm-wake-drain.sh
OPEN DECISIONS (still open, folded from the durable status logs - not just the latest line):
t31 needs-decision: ship now or wait [key=y]
OPEN DECISIONS: close one by answering it: bin/fm-send.sh <task> --resolve-key <key> '<answer>'
KEY SYNTAX (a stated decision key the fold could not use as written - not re-printed once recorded as warned):
t31 key token past the note head, so this line folded under "default": needs-decision: pick A or B [key=x]
t31 key token past the note head, so this line folded under "default": needs-decision: ship now or wait [key=y]
KEY SYNTAX: the documented position is before the colon - needs-decision [key=<slug>]: <summary>; steer the worker, and answer an affected decision by the key the open listing above actually shows.

### 3. Drain again with no new appends: the warning does NOT repeat
$ bin/fm-wake-drain.sh
OPEN DECISIONS (still open, folded from the durable status logs - not just the latest line):
t31 needs-decision: ship now or wait [key=y]
OPEN DECISIONS: close one by answering it: bin/fm-send.sh <task> --resolve-key <key> '<answer>'
(no KEY SYNTAX section - warned once, as designed)

### 4. An invalid slug (charset the fold rejects) never lists as open, but the guard still reports it
$ cat state/t32.status
needs-decision [key=bad/slug]: choose blue or green
$ bin/fm-wake-drain.sh
OPEN DECISIONS (still open, folded from the durable status logs - not just the latest line):
t31 needs-decision: ship now or wait [key=y]
OPEN DECISIONS: close one by answering it: bin/fm-send.sh <task> --resolve-key <key> '<answer>'
KEY SYNTAX (a stated decision key the fold could not use as written - not re-printed once recorded as warned):
t32 key slug outside A-Za-z0-9._-, so the fold skipped this line entirely: needs-decision [key=bad/slug]: choose blue or green
KEY SYNTAX: the documented position is before the colon - needs-decision [key=<slug>]: <summary>; steer the worker, and answer an affected decision by the key the open listing above actually shows.
